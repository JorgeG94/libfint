! GENERATED by scripts/rotaxis_mmd -- do not edit.
! ssss: L=0 comps=1 S=1 g=1 r=1 (terms 1) km=1 cm=1 assembly terms=1
module rotaxis_ssss_m
   use cint_const, only: dp
   use cint_rotaxis_boys, only: boys
   implicit none
   private
   public :: rotaxis_ssss
contains
   ! (ss|ss)  L=0: 1 bra accumulators, 1 ket accumulators, 1 components
   subroutine rotaxis_ssss(nbra, ncb, bp, kab, nket, nck, kp, kcd, gc, cutoff, res, any)
      integer,  intent(in)  :: nbra, ncb, nket, nck
      real(dp), intent(in)  :: bp(5, nbra), kab(1*ncb, nbra)
      real(dp), intent(in)  :: kp(7, nket), kcd(1*nck, nket), gc(3), cutoff
      real(dp), intent(out) :: res(1, ncb*nck)
      logical,  intent(out) :: any
      real(dp) :: s(1, ncb), r(1, ncb, nck), t(1)
      real(dp) :: f(0:0), b(0:0)
      real(dp) :: p, ip, za, zb, eab, q, iq, yc, yd, ax, az, ecd
      real(dp) :: zq, alpha, tt, w, m2a, Rs, Rc, cy
      real(dp) :: g(1), km(1), cm(1)
      integer :: kq, bq, cc, ck, n

      Rs = gc(1); Rc = gc(2); cy = gc(3)
      any = .false.
      r = 0.0_dp
      do kq = 1, nket
         q = kp(1,kq); iq = kp(2,kq); yc = kp(3,kq); yd = kp(4,kq)
         ax = kp(5,kq); az = kp(6,kq); ecd = kp(7,kq)
         s = 0.0_dp
         do bq = 1, nbra
            eab = bp(5,bq)
            if (eab + ecd > cutoff) cycle
            any = .true.
            p = bp(1,bq); ip = bp(2,bq); za = bp(3,bq); zb = bp(4,bq)
            zq = za - az
            alpha = p*q/(p + q)
            tt = alpha*(ax*ax + cy*cy + zq*zq)
            call boys(f, tt, 0)
            w = 1.0_dp/sqrt(p + q)
            m2a = -2.0_dp*alpha
            do n = 0, 0
               b(n) = w*f(n)
               w = w*m2a
            end do
            g(1) = 1.0_dp
            do cc = 1, ncb
               s(1,cc) = s(1,cc) + kab(cc,bq)*b(0)
            end do
         end do
         if (.not. any) cycle
         km(1) = 1.0_dp
         do cc = 1, ncb
            t(1) = s(1,cc)
            do ck = 1, nck
               r(1:1,cc,ck) = r(1:1,cc,ck) + kcd(ck,kq)*t(1:1)
            end do
         end do
      end do
      res = 0.0_dp
      if (.not. any) return
      cm(1) = 1.0_dp
      do ck = 1, nck
         do cc = 1, ncb
            res(1,cc+ncb*(ck-1)) = r(1,cc,ck)
         end do
      end do
   end subroutine rotaxis_ssss

end module rotaxis_ssss_m
